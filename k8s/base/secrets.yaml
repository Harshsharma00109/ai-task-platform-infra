apiVersion: v1
kind: Secret
metadata:
  name: neuralq-secrets
  namespace: neuralq
type: Opaque
# Values must be base64 encoded: echo -n 'value' | base64
# Replace these with actual production secrets via sealed-secrets or external-secrets-operator
stringData:
  JWT_SECRET: "REPLACE_WITH_STRONG_SECRET_MIN_32_CHARS"
  REDIS_PASSWORD: ""
  MONGODB_ROOT_PASSWORD: ""
